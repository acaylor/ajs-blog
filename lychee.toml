# Configuration for the `linkcheck` CI job (lychee).
# The previous baseline of pre-existing broken links has been cleared; all
# entries were addressed in a content pass on 2026-05-26. Add new excludes
# only as a last resort — prefer fixing the underlying link.

no_progress = true

# Temporary: /images/pg_logo.png is referenced as the hero image on every
# PostgreSQL post, but the asset has not been committed yet. Remove this
# entry once the file lands in public/images/.
exclude = [
  "/images/pg_logo\\.png",
]
